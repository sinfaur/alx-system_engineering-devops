#!/bin/bash

# Retrieve the active IPv4 addresses using ifconfig and filter out the loopback address
ips=$(ifconfig | grep 'inet ' | awk '{print $2}' | grep -v '127.0.0.1')

# Display the active IPv4 addresses
echo "$ips"

